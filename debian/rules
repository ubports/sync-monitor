#!/usr/bin/make -f
# -*- makefile -*-
export DPKG_GENSYMBOLS_CHECK_LEVEL=4

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --parallel --fail-missing --with translations

.PHONY: override_dh_strip
override_dh_strip:
	dh_strip --dbg-package=sync-monitor-dbg
